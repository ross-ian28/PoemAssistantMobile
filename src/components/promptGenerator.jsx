import { Text, View } from 'react-native';

export default function PromptGenerator({ navigation }) {
    return (
      <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
       <Text>Prompt Page</Text>
      </View>
    );
  }